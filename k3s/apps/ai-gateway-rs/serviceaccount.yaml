---
# ai-gateway Pod の ServiceAccount。
# GCP WIF Provider の attribute-condition で
#   namespace == 'ai-gateway' && serviceaccount.name == 'ai-gateway'
# が必須なので、namespace 名 / SA 名は固定。変更すると WIF が通らない。
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-gateway
  namespace: ai-gateway
