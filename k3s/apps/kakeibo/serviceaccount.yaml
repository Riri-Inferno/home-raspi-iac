---
# kakeibo backend Pod の ServiceAccount。
# GCP WIF Provider の attribute-condition で
#   namespace == 'kakeibo' && serviceaccount.name == 'kakeibo-backend'
# が必須なので、namespace 名 / SA 名は固定。変更すると WIF が通らない。
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kakeibo-backend
  namespace: kakeibo
